$(document).ready(function(){
	carousel.init({target:'slideshow-mazarino', type:'fade'});
	carousel.init({target:'slideshow-cocina', type:'fade',autoscroll:false});
	carousel.init({target:'slideshow-closet', type:'fade',autoscroll:false});
});
